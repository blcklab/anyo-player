# Player Web Surface example

This example proves the RC.11 host contract: Player installs Anyo's Web Surface plugin, registers trusted application code from `webSurface.apps`, resolves JSON-safe bindings from the world, and retains the declared snapshot for immersive XR or unsupported live presentation.

The world JSON contains no executable HTML or JavaScript. The host owns the application implementation and Player removes its convenience registration during disposal.
